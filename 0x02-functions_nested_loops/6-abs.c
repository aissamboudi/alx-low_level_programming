#include "main.h"
/**
 * _abs - Absolute value of an integer
 * @d: integer input
 * Return: absolute value d number
 */
int _abs(int d)
{
if (d > 0)
return (d * 1);
else if (d < 0)
return (d * -1);
else
return (0);
}
